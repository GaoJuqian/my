import { describe, expect, it } from "vitest";

describe("2.测试-测试", () => {
    it("should works", () => {
        expect(1 + 2).toEqual(3);
    });
});

describe("3.测试-测试", () => {
    it("should works", () => {
        expect(1 + 1).toEqual(2);
    });
});
